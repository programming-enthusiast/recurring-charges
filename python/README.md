# Recurring Charges Exercise (Python)

## Dependencies

* Python 3
* [pip](https://pip.pypa.io/en/stable/installing/)

## Installation

* `pip install pipenv` (if you don't have pipenv)
* `pipenv install`

## Tests

Run `pipenv run python test.py` to run the test suite
